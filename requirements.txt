# VERCEL-COMPATIBLE REQUIREMENTS
# Minimal dependencies for deployment (under 250MB limit)
# For full ML version, see requirements-original-full.txt

# Core framework
streamlit==1.28.1

# Data processing
pandas==2.1.3
numpy==1.24.3

# Image processing
pillow==10.1.0

# HTTP requests
requests==2.31.0

# Visualization
plotly==5.17.0

# Utilities
python-dotenv==1.0.0

# REMOVED FOR VERCEL (too large):
# - torch (>1GB) 
# - transformers (requires torch)
# - torchvision (requires torch)
# - sentence-transformers (requires torch)
# - speechrecognition (not needed for web)
# - pyttsx3 (not needed for web)
# - datasets (too large)
# - gradio (using streamlit only)
# - matplotlib (not essential)
# - seaborn (not essential)
# - opencv-python-headless (can be large)
# - scikit-learn (can add back if needed, ~100MB)

# NOTE: This creates a lightweight demo version
# For full AI features, deploy to Streamlit Cloud instead